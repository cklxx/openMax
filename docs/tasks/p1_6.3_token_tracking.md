# Task 6.3: Token Tracking

| Field | Value |
|-------|-------|
| **Priority** | P1 |
| **Dependencies** | — |
| **Status** | pending |

## Goal

Track token consumption per agent and overall session. Provide visibility into input/output token usage for cost awareness and optimization.

## Key Files

- `src/openmax/lead_agent/core.py`
- `src/openmax/session_runtime.py`

## Acceptance Criteria

- [ ] Track input/output tokens per agent from SDK responses
- [ ] Track cumulative session token totals
- [ ] Store token stats in session state (persisted across resume)
- [ ] Include token info in `report_completion` output
- [ ] Tests cover: token accumulation, per-agent tracking, session totals, report output

## Implementation Notes

- The `claude-agent-sdk` client loop in `core.py` receives usage metadata in responses — extract `input_tokens` and `output_tokens`
- Add token tracking fields to `LeadAgentRuntime` or session state: `token_usage: dict[str, dict[str, int]]` keyed by agent/pane ID
- Accumulate tokens on each SDK response in the client loop
- Add session-level totals: `total_input_tokens`, `total_output_tokens`
- Persist token stats via `session_runtime.py` session events or state
- Surface token summary in `report_completion` tool output

## Dispatch Strategy

- Agent count: 1
- Prompt key points: "Implement token tracking in `src/openmax/lead_agent/core.py` and `src/openmax/session_runtime.py`. Extract token usage from SDK responses. Track per-agent and session-level totals. Persist in session state. Add token info to report_completion. Add tests. Follow the spec in `docs/tasks/p1_6.3_token_tracking.md`. Run `ruff check` and `pytest` before committing."
