# 11.3 Cost Convergence & Budget Control

**Priority:** P2
**Status:** done

## Goal

Add per-subtask token budgets with soft/hard limits to prevent runaway costs.

## Acceptance Criteria

- [x] SubTask dataclass has `token_budget` (optional int) and `tokens_used` (int, default 0)
- [x] PlanSubmission has `total_budget` (optional int)
- [x] `dispatch_agent` accepts optional `token_budget` parameter
- [x] `_check_budget_warning` helper returns soft_limit at 80%, hard_limit at 100%
- [x] `read_pane_output` includes budget status in response when budget is set
- [x] LeadAgentRuntime has `token_usage` tracking dict
- [x] Tests cover budget warning thresholds and token_budget parameter acceptance
